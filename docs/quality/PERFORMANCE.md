# Performance

Use the shared performance budget. Measure representative mobile and desktop routes with production assets and realistic content. Track JavaScript, CSS, fonts, images, LCP, CLS, INP, caching, and API latency separately.

Large hero imagery and interactive graph/timeline modules load progressively and must not block primary content. Any budget exception needs measured evidence, an owner, and an expiry condition.
