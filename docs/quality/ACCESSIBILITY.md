# Accessibility

Target WCAG 2.2 AA. Required evidence includes keyboard-only use, logical focus order, visible focus, skip navigation, landmarks, heading structure, form labels and errors, screen-reader spot checks, 200% and 400% zoom, reflow, contrast, RTL reading order, language changes, reduced motion, and non-color status cues.

Automated scans are necessary but not sufficient for acceptance.
