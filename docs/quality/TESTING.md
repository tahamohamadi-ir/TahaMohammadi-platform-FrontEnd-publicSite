# Testing

The selected scaffold must provide formatting, lint, static types, unit tests, component tests, production build, API contract tests, and browser tests. Browser coverage includes the gateway, locale switching, global navigation, representative page families, contact, search, not-found, API failure, and recovery.

Fixtures cover both locales and all content states. Tests must assert semantic outcomes and accessible names, not fragile implementation detail.
